apply plugin: 'maven-publish'

publishing {
    publications {
        release(MavenPublication) {
            from components.java
            groupId 'com.github.qiupengfei10'
            artifactId 'XmlClassGuard'
            version "$plugin_version"
        }
    }
}